if SHIELD_TITAN8000

config ZMK_KEYBOARD_NAME
	default "Titan 8000"

config TITAN8000_BUZZER
	bool "Enable buzzer support for Titan8000"
	default n

config PWM
    default y if TITAN8000_BUZZER

endif